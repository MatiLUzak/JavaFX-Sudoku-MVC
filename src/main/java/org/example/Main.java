package org.example;

// Press Shift twice to open the Search Everywhere dialog and type `show whitespaces`,
// then press Enter. You can now see whitespace characters in your code.
public class Main {
    /*public static void main(String[] args) {
        SudokuSolver solver = new BacktrackingSudokuSolver();
        SudokuBoard board = new SudokuBoard(solver);
        board.solveGame();
        boolean isBoardValid = board.checkBoard();
        System.out.println("Is the Sudoku board valid? " + isBoardValid);

        // Wydrukowanie planszy do konsoli
        for (int i = 0; i < SudokuBoard.GRID_SIZE; i++) {
            for (int j = 0; j < SudokuBoard.GRID_SIZE; j++) {
                System.out.print(board.get(i, j) + " ");
            }
            System.out.println();
        }
    }*/

}